@model CFProject_T6.Models.Packages

@{
    ViewData["Title"] = "Package Details";
}

<h2>Package Details</h2>

<div>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            Donation Amount
        </dt>
        <dd>
            @Html.DisplayFor(model => model.DonationUpperlim)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.Reward)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Reward)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.Project)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Project.Title)
        </dd>
    </dl>
</div>
<div>
    @*<form asp-controller="BackersProjects" asp-action="purchase">
            <input type="submit" value="Purchase" class="btn btn-default" />
        </form>*@

    @*<a href="@Url.RouteUrl("purchase", new { controller = "BackersProjects", action = "Create"})">Purchase</a>*@
        
    @*<a href="@Url.RouteUrl("purchase", new { })"> Purchase </a>*@

    <a href="@Url.Action("create", "BackersProjects", new { project_id = Model.ProjectId, package_id = Model.Id })">Purchase</a> |
    @*<a asp-controller="BackersProjects" asp-action="purchase">Purchase!</a> |*@
    <a asp-action="Edit" asp-route-id="@Model.Id">Edit</a> |
    <a asp-action="Index">Back to List</a>
</div>
